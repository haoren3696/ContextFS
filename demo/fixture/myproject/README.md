# myproject

A tiny Rust project used as a fixture for the agentvfs quickstart demo.

Not meant to be built — its only job is to look like a real project tree
when an agent first lands inside the agentvfs mount.
